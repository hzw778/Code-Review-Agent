package com.codereview.agent.git;

/**
 * Diff 解析器（预留）
 * 当前版本 diff 解析已内置在 GitOperationService 中（使用 JGit 的 DiffFormatter）。
 * 后续如需解析命令行 git diff 输出，可在此实现。
 *
 */
public class DiffParser {
    // 预留，暂不实现
}